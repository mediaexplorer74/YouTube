<Page
    x:Class="YouTube.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- NavBar -->
        <local:navbar x:Name="navbar" Grid.Row="0"/>

        <!-- Content area -->
        <Grid x:Name="ContentArea" Grid.Row="1">
            <local:Home x:Name="homepage"/>
            <local:Search x:Name="searchpage" Visibility="Collapsed"/>
            <local:Shorts x:Name="shortspage" Visibility="Collapsed"/>
            <local:Subscriptions x:Name="subscriptionspage" Visibility="Collapsed"/>
        </Grid>

        <!-- TabBar -->
        <local:tabbar x:Name="tabbar" Grid.Row="2"/>
    </Grid>
</Page>
