<Page
    x:Class="YouTube.Subscriptions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:YouTube.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <DataTemplate x:Key="VideoItemTemplate" x:DataType="data:VideoInfo">
            <Button Style="{StaticResource VideoCardButtonStyle}" Click="VideoCard_Click" Tag="{Binding video_id}">
                <Grid Width="280" Height="250" Margin="0,0,10,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="165"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" CornerRadius="8">
                        <Border.Clip>
                            <RectangleGeometry Rect="0,0,280,165" />
                        </Border.Clip>
                        <Image Source="{x:Bind thumbnail}" Stretch="UniformToFill" VerticalAlignment="Top" />
                    </Border>

                    <StackPanel Grid.Row="1" Margin="0,8,0,0">
                        <TextBlock Text="{x:Bind title}"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   FontSize="15"
                                   MaxLines="2"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Top"/>
                        <TextBlock Text="{x:Bind author}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{StaticResource SystemBaseMediumLowColorBrush}"
                                   Margin="0,2,0,0"/>

                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="{x:Bind Views}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{StaticResource SystemBaseMediumLowColorBrush}"/>
                            <TextBlock Text=" • " Style="{StaticResource CaptionTextBlockStyle}" Foreground="{StaticResource SystemBaseMediumLowColorBrush}"/>
                            <TextBlock Text="{x:Bind PublishedAt}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{StaticResource SystemBaseMediumLowColorBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Button>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:navbar x:Name="navbar" Grid.Row="0" />

        <ScrollViewer Grid.Row="1" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="10">
                <TextBlock Text="Subscriptions" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

                <ProgressRing x:Name="LoadingRing"
                              IsActive="False"
                              Width="40"
                              Height="40"
                              Margin="0,20,0,0"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>

                <GridView x:Name="SubscriptionsGridView"
                          ItemsSource="{x:Bind SubscriptionVideos}"
                          ItemTemplate="{StaticResource VideoItemTemplate}"
                          IsItemClickEnabled="True"
                          ItemClick="SubscriptionsGridView_ItemClick"
                          SelectionMode="None"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </StackPanel>
        </ScrollViewer>

        <local:tabbar x:Name="tabbar" Grid.Row="2" />
    </Grid>
</Page>
