<Page
    x:Class="YouTube.History"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:YouTube.Converters"
    mc:Ignorable="d"
    Background="Black">

    <Page.Resources>
        <local:StringIsNullOrEmptyToVisibilityConverter x:Key="StringIsNullOrEmptyToVisibilityConverter"/>
        <converters:ImageSourceConverter x:Key="ImageSourceConverter"/>
    </Page.Resources>

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- NavBar -->
        <local:navbar x:Name="navbar" Grid.Row="0"/>

        <!-- Content area -->
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <TextBlock Text="История просмотров"
                          FontSize="24"
                          FontWeight="Bold"
                          Margin="20,20,20,20"
                          Foreground="White"/>
                          
                <ListView x:Name="HistoryContainer"
                         Background="Black"
                         SelectionMode="None"
                         IsItemClickEnabled="True"
                         ItemClick="VideoCard_Click"
                         Margin="0,0,0,0"
                         HorizontalAlignment="Stretch">

                    <!-- Items Panel Template for multi-column layout -->
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" 
                                         ItemWidth="360"
                                         MaximumRowsOrColumns="3"
                                         HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" 
                                      Margin="0,0,0,24" 
                                      Background="Black" 
                                      HorizontalAlignment="Center"
                                      Width="360">
                                <!-- Video Thumbnail -->
                                <Grid>
                                    <Border CornerRadius="8" Background="#1A1A1A" Height="202">
                                        <Image Source="{Binding thumbnail}" 
                                               Stretch="UniformToFill" 
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                    </Border>
                                </Grid>

                                <!-- Channel Thumbnail, Title, and Author -->
                                <StackPanel Orientation="Horizontal" Margin="12,8,12,0">
                                    <!-- Channel Thumbnail -->
                                    <Border Width="36" Height="36" CornerRadius="18" Background="#333333"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding channel_thumbnail, Converter={StaticResource StringIsNullOrEmptyToVisibilityConverter}}">
                                        <Image Source="{Binding channel_thumbnail, Converter={StaticResource ImageSourceConverter}, FallbackValue='ms-appx:///Assets/placeholder.png'}"
                                               Stretch="UniformToFill"/>
                                    </Border>
                                    
                                    <!-- Title and Author -->
                                    <StackPanel Orientation="Vertical" Margin="12,0,0,0">
                                        <TextBlock Text="{Binding title}" 
                                                 Foreground="White" 
                                                 FontSize="16" 
                                                 FontWeight="Bold" 
                                                 TextWrapping="WrapWholeWords" 
                                                 MaxLines="2" 
                                                 TextTrimming="WordEllipsis"/>
                                        <TextBlock Text="{Binding author}" 
                                                 Foreground="#FFAAAAAA" 
                                                 FontSize="12" 
                                                 Margin="0,2,0,0"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Views}" 
                                                     Foreground="#FFAAAAAA" 
                                                     FontSize="12"/>
                                            <TextBlock Text=" • " 
                                                     Margin="5,0" 
                                                     Foreground="#FFAAAAAA" 
                                                     FontSize="12"/>
                                            <TextBlock Text="{Binding PublishedAt}" 
                                                     Foreground="#FFAAAAAA" 
                                                     FontSize="12"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>

        <!-- TabBar -->
        <local:tabbar x:Name="tabbar" Grid.Row="2"/>
    </Grid>
</Page>
