<Page
    x:Class="YouTube.AccountLogin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YouTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black">

    <Grid Background="Black" x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:navbar x:Name="navbar" Grid.Row="0"/>

        <StackPanel x:Name="qrContainer" 
                    Grid.Row="1"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="20,0,20,0">

            <TextBlock x:Name="qrText"
                        Text="Для входа отсканируйте QR-code"
                        FontSize="18"
                        Foreground="White"
                        HorizontalAlignment="Center"
                        TextWrapping="Wrap"
                        TextAlignment="Center"
                        Margin="0,0,0,20"/>

            <Border Background="White" 
                    CornerRadius="8" 
                    Padding="10" 
                    MaxWidth="300" 
                    MaxHeight="300">
                <Image x:Name="authImage" 
                        Stretch="Uniform" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"/>
            </Border>

            <Button x:Name="settingsButton"
                    Content="Настройки"
                    Background="#1a1a1a"
                    Foreground="White"
                    BorderThickness="0"
                    Padding="16,8"
                    HorizontalAlignment="Center"
                    Margin="0,30,0,0"
                    Click="SettingsButton_Click"/>
        </StackPanel>

        <local:tabbar x:Name="tabbar" Grid.Row="2"/>
    </Grid>
</Page>